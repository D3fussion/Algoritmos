Primero, si no tienes instalado uv, instalalo con este comando en MacOS:

brew install uv

Luego, instala el entorno virtual con este comando:

uv sync

Finalmente, ejecuta el servidor con este comando:

uv run app.py
